# =============================================================================
# SZABLON KONFIGURACJI (.env.example)
# =============================================================================
# Ten plik służy jako dokumentacja (ściąga) wszystkich dostępnych parametrów.
# Zaleca się podział konfiguracji na dwa pliki:
# 1. .env -> Dane wrażliwe (tokeny, hasła) i specyficzne dla środowiska.
# 2. local_config.json -> Logika biznesowa, listy instrumentów, limity.
# =============================================================================

# --- SEKJA 1: PLIK .env (Wymagane) ---
# Skopiuj poniższe wartości do pliku .env i uzupełnij swoimi danymi.

# Token bota Telegrama (od @BotFather)
TELEGRAM_BOT_TOKEN="123456789:ABCdefGHIjklMNOpqrsTUVwxyz"

# Twoje ID na Telegramie (do powiadomień admina)
TELEGRAM_CHAT_ID="123456789"

# Środowisko: 'production' (handel) lub 'practice' (testy/logi)
ENVIRONMENT=practice

# Adres serwera ML (opcjonalne, jeśli używasz AI)
ML_BASE_URL=""


# --- SEKCJA 2: PLIK local_config.json (Zalecane) ---
# Poniższe parametry możesz ustawić w .env, ale ZALECAMY utworzenie pliku
# `local_config.json` w głównym katalogu, aby łatwiej zarządzać listami i liczbami.
# Plik JSON ma priorytet nad .env.

# Przykładowa struktura local_config.json:
# {
#   "MODE": "advisor",
#   "DATA_SOURCE": "yahoo",
#   "INSTRUMENTS": "EURUSD=X,GBPUSD=X,BTC-USD",
#   "TIMEFRAME": "H1",
#   "RISK_PER_TRADE_PERCENT": 1.0,
#   "MAX_TRADES_PER_DAY": 5,
#   "EDUCATIONAL_MODE": "true"
# }

# --- OPIS PARAMETRÓW (do użycia w local_config.json lub .env) ---

# Tryb działania bota:
# 'advisor'     -> Tylko wysyła sygnały na Telegram (bezpieczne)
# 'auto'        -> Automatycznie zawiera transakcje (wymaga implementacji brokera)
# 'signal_only' -> Tylko loguje sygnały w konsoli/plikach
MODE=advisor

# Źródło danych rynkowych:
# 'yahoo'       -> Darmowe dane z Yahoo Finance (opóźnione/nieprecyzyjne)
# 'oanda'       -> Broker OANDA (wymaga implementacji klienta)
DATA_SOURCE=yahoo

# Interwał czasowy świec:
# 'M1', 'M5', 'M15', 'H1', 'H4', 'D1'
TIMEFRAME=H1

# Waluta bazowa konta (do obliczeń ryzyka)
BASE_CURRENCY=USD

# Ryzyko na pojedynczą transakcję (w % kapitału)
RISK_PER_TRADE_PERCENT=1.0

# Limit liczby transakcji dziennie (zabezpieczenie przed overtradingiem)
MAX_TRADES_PER_DAY=5

# Limit transakcji na jeden instrument dziennie
MAX_TRADES_PER_INSTRUMENT_PER_DAY=3

# Czy włączyć tryb edukacyjny? (True/False)
# Dodaje wyjaśnienia i definicje do sygnałów na Telegramie.
EDUCATIONAL_MODE=True

# Lista instrumentów (rozdzielona przecinkami):
# Jeśli puste -> używa domyślnego koszyka zdefiniowanego w kodzie.
INSTRUMENTS=EURUSD=X,GBPUSD=X,BTC-USD,GC=F

# Alternatywnie: Sekcje instrumentów (zdefiniowane w app/data/instrument_universe.py)
# Np.: forex_majors, crypto_majors
INSTRUMENT_SECTIONS=

# Częstotliwość sprawdzania cen (w sekundach)
DATA_POLL_INTERVAL_SECONDS=60

# --- Filtry Backtestów / Live (Zaawansowane) ---
# Minimalna oczekiwana wartość (Expectancy) z backtestów, aby dopuścić instrument do handlu
LIVE_MIN_EXPECTANCY_1D=0.0
BACKTEST_MIN_EXPECTANCY_1D=0.0
# Długość okna dla wyższego interwału (HTF)
BACKTEST_HTF_WINDOW=100
# Ilość świec do pobrania przy backteście
BACKTEST_1D_COUNT=10000
